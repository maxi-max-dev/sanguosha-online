# 1v1 单挑模式 · 施工单

**动机**：身份局最少 5 人。Max 和朋友经常凑不齐——两三个人的时候现在完全没得玩。
这是使用频率最高的缺口，不是锦上添花。

---

## 规则（就按这版做，别自由发挥）

- **2 人对局**。
- **各选 3 名武将**，从各自的 3 个候选里依次挑（不是一次给 9 个）。
- **出场顺序由自己定**：选完后排一个顺序，第一名为首发。
- **先手方开局摸 4 张，后手方摸 6 张** —— 补偿先手优势，这是单挑的标准做法，别省。
- **当前武将阵亡后换下一名**，新武将**体力回满**、手牌重新摸 4 张、装备判定区清空。
- **三名武将全部阵亡则败**。
- 牌堆、卡牌规则完全沿用军争篇，不改。

## 关键设计难点（先想清楚再动手）

现有引擎里"阵亡"是终局的：`PlayerState.alive = false`，`checkWin()` 判胜负。
单挑要的是**换将**——同一个座位换一个武将继续打。这是本次唯一的架构问题。

`IdentityGame` 的做法是覆写 `onDeathReward()` 和 `checkWin()`。单挑需要更深一层：
**在角色真正"死透"之前拦截**。建议方案（自行判断是否更优）：

- 新建 `packages/engine/src/modes/duel.ts`，`class DuelGame extends Game`
- 覆写 `Game.die()`：先看这个座位还有没有替补武将
  - 有 → 换将（重置 general/skills/maxHp/hp、清空区域、重新发牌），**不置 `alive = false`**，
    推一条 `{kind:'switchGeneral'}` 日志给前端播动画
  - 没有 → 走原本的 `super.die()`
- 覆写 `checkWin()`：某一方三将出尽即负

**硬约束照旧**（见 CLAUDE.md）：随机只走 `this.rng`、玩家输入只走 `this.ask`。
换将时抽新武将、重新发牌都必须走这两条路，否则重放会静默错位。

## 前端

- 房间层：开局人数校验现在写死 5–8（`apps/server/src/room.ts` 的 `MIN_PLAYERS`），
  要按模式区分。大厅需要一个模式选择（身份局 / 单挑）。
- 选将：复用现成的 `GeneralPicker`，但要选 3 个并排序。
- 牌桌：2 人局的座位布局要单独处理（现在是"对手环绕顶部"，2 人时应该是面对面）。
  另外要显示对方**还剩几名武将**，这是单挑最重要的局势信息。
- 换将时的过场：至少要有明确提示"XXX 阵亡，换上 YYY"。

## 验收（拿不出证据的"应该好了"视为没做）

```bash
pnpm -r exec tsc -p tsconfig.json --noEmit
pnpm test                                    # 43 单测 + 6 端到端，不能变红
pnpm --filter @sgs/engine soak 500 5          # 身份局不能被改坏
pnpm --filter @sgs/engine soak 500 8
```

单挑模式自己的验收：
1. `soak` 要支持单挑（加一个 mode 参数），**500 局零崩溃**
2. 断言：一方三将出尽即判负；换将后体力回满、手牌为 4
3. 端到端：2 人建房 → 选将 → 开局 → 换将 → 分胜负

**不要部署**，交给我验收后统一上线。
