# 产品诊断与施工单

目标：从"能跑通"做到"朋友拿起来就会玩、玩得下去、愿意再来一局"。

本文是**施工依据**。每一项都写清楚了根因和验收标准 —— 不要凭感觉改，
改完必须能对着"验收"那一栏证明它好了。

---

## 一、真人反馈（最高优先级，全部已复现）

### A1. 【过河拆桥】【顺手牵羊】点不动，只能等超时 —— 🔴 整块 UI 缺失

**现象**：轮到你拆牌，提示出来了，但点谁的牌都没反应，干等到超时被系统随机拆掉。

**根因**：`AskRequest` 里的 `chooseCards` 这个类型，**前端从来没有渲染过它的候选牌**。
`Table.tsx` 的 `Hand` 组件只画 `me.hand`，而 `chooseCards.candidates` 里装的是
**别人**的手牌/装备/判定区的牌（带 `unknown` / `from` / `zone` 字段）。
`isReady()` 里有对它的校验，但界面上压根没有可点的东西。

**波及范围**（全部同样点不动）：过河拆桥、顺手牵羊、火攻、五谷丰登、
司马懿反馈、郭嘉遗计的分配、以及所有走 `chooseCards` 的技能。

**要做**：新增一个"选牌浮层"，覆盖 `chooseCards` 和 `distribute` 两种请求。
- 候选牌按来源分组展示（"机器人1 的手牌 / 装备区 / 判定区"）
- `unknown: true` 的牌画成牌背，只按位置区分，不能透露牌面（**这是防作弊边界，不能图省事直接显示**）
- 选够 `min` 张才允许确定；`max > min` 时允许多选
- `distribute` 类型要能把每张牌拖/点给指定角色

**验收**：8 人局里用过河拆桥拆掉指定角色的指定位置的牌，全程不超时；
拆"未知手牌"时界面上不出现任何牌面信息（用 `view.cards` 里没有该 id 来证明）。

---

### A2. 出牌还要再点一次自己 —— 🟠 多余的一步

**现象**：用【桃】要先点牌、再点自己的武将牌、再点确定。三步。

**根因**：`options.ts` 的 `targetsFor()` 对【桃】算出 `min:1, max:1, candidates:[自己]`，
`auto` 是 false，于是前端要求手动选目标。但**合法目标只有一个，选择本身不存在**。

**要做**：在 `targetsFor()` 里，当 `candidates.length === min && min === max` 时置 `auto: true`。
这是服务端的事，不要在前端绕过去（规则只有一份实现，见 CLAUDE.md）。

**验收**：桃、酒、装备牌都是"点牌 → 确定"两步；杀、决斗这类多目标的仍然要选人。

---

### A3. 【酒】不知道怎么用 —— 🟠 A2 的直接后果 + 缺状态提示

**现象**：点了酒没反应，以为坏了。

**根因**：两层。① 酒的目标是自己，撞上 A2；② 酒生效后只在服务端记了
`turn:jiuActive` 标记，**界面上没有任何地方显示"你现在处于酒状态"**，
玩家不知道它到底生效没有、下一张杀会不会加伤。

**要做**：A2 修完自然解决第一层。第二层要在自己的武将牌上显示状态标记
（`PlayerView.marks` 已经在下发 `turn:` 前缀的 flag 了，前端只是没用）。
至少显示：酒、已用杀次数/上限、限定技是否用过。

**验收**：用酒之后武将牌上出现"酒"标记，打出杀之后标记消失，且那一杀确实造成 2 点伤害。

### A4. 【观星】点不动 —— 🔴 又一块 UI 缺失（同 A1 病因）

**现象**：诸葛亮回合开始发动观星，界面上没有任何可操作的东西，只能卡到超时被系统随机排。

**根因**：`arrange` 请求（观星专用：看牌堆顶 X 张、重排到牌堆顶/底）前端零 UI，
和 A1 的 `chooseCards` 是同一个病：服务端算对了、下发了，前端没地方点。
诸葛亮是标准包里最常被选的武将之一，招牌技能等于不存在。

**要做**：排序浮层。展示这 X 张牌，允许玩家把每张分到"牌堆顶"或"牌堆底"两个区，
并能调整牌堆顶那组的先后顺序（这个顺序有意义：先摸到的在前）。
交互不必做拖拽，点选 + 上下移按钮就够。

**验收**：用诸葛亮开局，观星把已知的某张牌放到牌堆顶第一位，下一次摸牌确实摸到它。

---

### A5. 转化技点不出来 —— 🔴 六个武将的看家本领不可用

**现象**：关羽手里一张红【桃】，想用武圣当【杀】出——点那张桃，只有"用桃"这一个用法，
没有任何入口能选到"当杀"。甘宁的奇袭、赵云的龙胆、甄姬的倾国、大乔的国色、
华佗的急救，全部一样。

**根因**：`apps/web/src/store.ts` 的 `optionForCard()` 写的是
`mine.find(o => !o.recast) ?? mine[0]` —— 一张牌对应多个出牌选项时，
**永远只返回第一个**。服务端其实把转化选项都算好下发了（`options` 里有
`skill:wusheng:<id>:sha` 这样的条目），前端只是从来没给过第二个选项的入口。

**波及**：武圣（关羽）、龙胆（赵云）、倾国（甄姬）、奇袭（甘宁）、
国色（大乔）、急救（华佗）—— 六个武将的核心技能。

**要做**：一张牌有多个用法时，点击后要能选用法。建议：点牌后在牌上方浮出一排
"用法"按钮（"当【杀】" / "当【过河拆桥】" / "重铸"），点哪个用哪个。
只有一个用法时保持现在的行为，不要多一步。

**验收**：用关羽，手里一张红色非杀的牌，能通过武圣把它当【杀】打出去，
战报里出现"关羽 使用【杀】"且消耗的是那张红牌。

---

## 二、地基（做长期的东西必须先稳的）

引擎的核心约束见 `CLAUDE.md`，那条不重复。这里列**目前地基上真实的薄弱点**：

### B1. 没有服务端可导出的对局记录 —— 🟠

决策日志已经落在 DO 的 SQLite 里了（这是事件溯源架构的自然产物），
但**没有任何办法把它取出来**。后果：朋友说"刚才那局有个地方不对"，
我们除了猜没有别的办法。

**要做**：加一个只读导出接口 `GET /api/room/:code/replay`，返回
`{ seed, setup, decisions }`（即 `GameRecord`）。有了它：
- 任何 bug 都能本地精确重放（`CLAUDE.md` 末尾已有重放代码片段）
- 未来做"观战回放"只是前端的事，服务端不用改

**注意**：`GameRecord` 含完整信息（所有人的身份和手牌），
**对局进行中绝不能导出**，只允许在 `state.finished` 之后取。这是防作弊边界。

**验收**：打完一局，拉取接口拿到 JSON，用它在本地 `new IdentityGame(record, registry)`
重放，最终 `state.finished` 与线上一致。

### B2. 前端没有错误边界 —— 🟠

任何一个渲染异常（比如某张牌的 id 在 `view.cards` 里不存在）会白屏整局，
而且因为状态在服务端，刷新能回来 —— 但玩家不知道该刷新。

**要做**：React 错误边界，兜底 UI 给一个"重新连接"按钮。

**验收**：人为抛错后看到兜底界面而不是白屏，点重连能回到牌局。

### B3. 没有任何自动化的端到端测试 —— 🟠

现在的保障是 42 项单测 + 无头压测，覆盖的是**引擎**。
Worker/DO 层和前端交互（连接、重连、托管、再来一局）全靠手工点。
这一轮就因此漏掉过两个回归（读秒续命、休眠后托管失效）。

**要做**：至少给 DO 层加测试（`wrangler` 有 `unstable_dev` 可以起本地实例），
覆盖：进房 → 开局 → 决策 → 断线重连拿回同样的手牌 → 超时托管 → 再来一局。

**验收**：`pnpm test` 一条命令跑完引擎单测 + DO 端到端测试。

### B4. 8 人局仍偏反贼、6 人局偏得更多 —— 🟡

当前机器人对局：5 人 主公方52/反贼41/内奸7，6 人 38/58/4，8 人 45/52/3。
6 人局是最偏的。只在机器人占多数时体现，真人局不受影响，优先级低于上面几条。

---

## 三、施工纪律

1. **改完要能证明。** 每一项都有"验收"，做完在报告里对着写。
   拿不出证据的"应该好了"一律视为没做。
2. **不许动引擎的硬约束**（随机只走 `rng`、输入只走 `ask`）。见 `CLAUDE.md`。
3. **规则判断只在服务端。** 前端发现"我这也得判断一下"就是设计错了，
   应该让服务端多下发一个字段。
4. **每改完一项就跑回归**：
   ```bash
   pnpm -r exec tsc -p tsconfig.json --noEmit
   pnpm --filter @sgs/engine test
   pnpm soak 500 5 && pnpm soak 500 8
   ```
   全绿才算过。
5. **不要自己部署**，改完交给我验收后统一上线。
